# Context Map

This repo has multiple contexts. Each context has its own `CONTEXT.md` and `docs/adr/` directory.

## Contexts

- **credit-system** (root): Main credit system context. See `CONTEXT.md` and `docs/adr/`.